# 2026-09-15

Prepared the Codex-first Bot 1.0.0 release. Added the logo, public install
instructions, verified download scripts, hosted binary workflow, and release
checks. Kept the imported Grok Build baseline and license notices identified in
`NOTICE`.

Removed the build-on-launch shortcut and unused first TUI prototype. Set Codex
as the production default. Kept Grok selected in its imported isolated test
environments, and repaired inherited build and test failures that blocked the
whole-workspace checks.

The public release gate requires a verified, signed one-commit history and a
five-target binary matrix. Native macOS and Windows publisher signing remains
outside the first release; `docs/release-status.md` records that limit.

Replaced the single-letter README badge with an SVG BOT wordmark.
Redrew the full and compact terminal art with matching proportions and a cursor
after the T. The startup welcome snapshot covers the change.

Matched the README wordmark to the TUI welcome panel: #141414 background,
#333333 border, and #6D6D6D letters. Kept the terminal art unchanged. Firefox
rendered the SVG, and XML parsing passed.
